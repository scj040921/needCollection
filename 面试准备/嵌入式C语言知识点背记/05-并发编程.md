---
title: 并发编程速记
tags: [面试准备, Linux, 并发, 嵌入式]
---

# 并发编程

## 进程、线程与生命周期

| 概念 | 记忆 |
| --- | --- |
| 进程 | 资源分配的基本单位，有独立虚拟地址空间；隔离好，切换/通信更重 |
| 线程 | CPU 调度单位，同进程线程共享代码、堆、全局变量和 fd；各自有栈、寄存器状态 |
| `fork()` | 创建子进程；父进程得子 PID，子进程得 0，失败为 -1；现代系统常写时复制 |
| `wait()/waitpid()` | 回收子进程，避免僵尸；`waitpid` 可指定 PID/非阻塞 |
| 僵尸 | 子进程已退出但父未回收，保留退出状态；不是“还在运行” |
| 孤儿 | 父先退出，子进程被收养；不等于僵尸 |

## 线程同步：先找共享状态

- **竞态条件**：结果依赖不可预测的执行交错，例如 `count++` 不是原子操作。
- 互斥锁保护临界区：锁的范围只覆盖共享数据一致性所需代码；所有访问路径使用同一把锁。
- 条件变量用于“等待条件成立”，必须配合 mutex 与 `while (!condition) pthread_cond_wait(...)`，避免虚假唤醒/抢占后条件又变假。
- 读写锁适合读多写少；自旋锁适合持锁极短且不能睡眠的场合，单核或竞争重时可能更差。

```c
pthread_mutex_lock(&lock);
while (queue_empty())
    pthread_cond_wait(&not_empty, &lock);
item = pop();
pthread_mutex_unlock(&lock);
```

> [!warning] 死锁四条件
> 互斥、占有且等待、不可抢占、循环等待。工程上固定加锁顺序、缩小持锁范围、统一异常路径解锁。

## 信号与 IPC

- 信号是异步通知；处理函数只能做异步信号安全的极少操作，常见安全做法是设置 `volatile sig_atomic_t` 标志或写管道，主循环再处理。
- `SIGKILL`、`SIGSTOP` 不能捕获/忽略；`kill(pid, sig)` 是发信号，不等于一定结束进程。
- 匿名管道适合有亲缘关系进程的单向字节流；命名管道可用于无亲缘进程；消息队列保留消息边界；共享内存最快但需自行同步。
- POSIX 信号量用于计数/同步；互斥锁强调所有权（通常谁加锁谁解锁），二者不要混为一谈。

## 定时器

- 相对延时易累计漂移；周期任务常采用“下一个绝对截止时间”计算。
- 定时器回调要短，不阻塞，不直接做复杂 IO；把工作交给线程/事件循环。

> [!question] 自测
> 为什么 `if` 不够而条件变量要用 `while`？醒来后锁虽重新拿到，但条件可能被其他线程先改变，且还可能虚假唤醒。

## 图片题目逐项补全

### `wait`、管道与消息队列

- `wait(NULL)` 等待任一子进程；`waitpid(pid, &status, options)` 可等待指定子进程，`WNOHANG` 让它不阻塞。两者都用于取得退出状态并回收资源。
- 匿名管道由 `pipe()` 创建，常用于亲缘进程；读取端在所有写端关闭且数据读尽时得到 EOF。每个进程要关闭自己不用的端，否则读端可能永远等不到 EOF。
- 命名管道 FIFO 有路径名，可供无亲缘进程打开；打开端的阻塞语义需要按读写方向理解。
- 消息队列保存离散消息并可按类型接收，适合任务消息；消息大小、队列容量和权限是资源上限，满队列时发送可能阻塞/失败。

### 信号、线程属性与可见性

- 信号有编号、默认动作、阻塞掩码和待处理状态；同类普通信号可能合并，不能把它当可靠计数器。扩展实时信号才更强调排队语义。
- `sigaction` 比 `signal` 更可控；处理函数只做安全的通知。被信号中断的系统调用可能返回 `EINTR`，调用方要决定重试或退出。
- 线程属性可设置栈大小、分离状态等；嵌入式需明确栈上界。可连接线程应 `pthread_join` 回收，分离线程退出后自动释放线程资源，不能再 join。
- 线程启动函数签名是 `void *(*start_routine)(void *)`；传入参数地址要保证在线程读取前仍有效，局部循环变量地址是常见坑。

### 条件变量、信号量与饥饿

- 条件变量解决“等某个共享状态变化”，本身不保存计数；信号量有计数，`sem_wait` 消耗、`sem_post` 增加，适用于资源数量/生产消费通知。
- 饥饿是某线程长期得不到运行/锁/资源，不等于死锁。可通过公平队列、限制读者连续占用、缩短临界区和合理优先级缓解；实时系统还要警惕优先级反转。
