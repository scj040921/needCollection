---
title: 数据结构速记
tags: [面试准备, 数据结构, C语言]
---

# 数据结构

## 线性表：先辨访问模式

| 结构 | 优势 | 代价 | 适合 |
| --- | --- | --- | --- |
| 顺序表/数组 | 下标随机访问 `O(1)`，缓存友好 | 中间插删要搬移 `O(n)`，容量受限 | 固定表、查找多 |
| 单链表 | 已知节点前驱时插删 `O(1)`，可动态增长 | 查找 `O(n)`，额外指针且缓存较差 | 插删多、节点大小不定 |
| 栈 | 后进先出，压栈/出栈 `O(1)` | 只能访问栈顶 | 函数调用、括号匹配、DFS |
| 队列 | 先进先出，入队/出队 `O(1)` | 不能随机访问 | 缓冲区、任务调度、BFS |

- 循环队列用“空一格”区分空/满：空 `front == rear`；满 `(rear + 1) % N == front`。
- 单链表删除节点前先保存 `next`；反转链表核心是维护 `prev/current/next` 三个指针。
- 链表遍历结束后必须把临时指针置空吗？不是必须；关键是不能丢失待释放链表头，也不能释放后解引用。

## 时间换空间、空间换时间

- 哈希表用额外桶/数组把平均查找降到 `O(1)`，代价是内存与碰撞处理。
- 缓存、查表、预分配常用空间换时间；压缩、流式处理常用时间换空间。
- 嵌入式优先先算上界：元素数量、每个节点大小、最坏碰撞/最坏递归深度，避免“平均情况很好”却内存耗尽。

## 二叉树与遍历

- 二叉树每个节点最多两个孩子；二叉搜索树（BST）额外满足左小右大。
- 前序：根左右；中序：左根右（BST 中序得到有序序列）；后序：左右根；层序：借助队列。
- 平衡树通过控制高度避免 BST 退化成链表；无需把所有树都背成代码，先说明为何要平衡。

## 排序与复杂度

| 算法 | 平均/最坏 | 特点 |
| --- | --- | --- |
| 冒泡 | `O(n²)` | 相邻交换，教学用 |
| 选择 | `O(n²)` | 交换较少，不稳定 |
| 插入 | 最坏 `O(n²)` | 近乎有序时快、稳定 |
| 快排 | 平均 `O(n log n)`，最坏 `O(n²)` | 原地快，选枢轴防退化 |
| 归并 | `O(n log n)` | 稳定，需额外空间 |

> [!tip] 面试表达
> 选择结构先说“访问、插入、删除各占多少”，再结合内存上限和实时性；不要只背 Big-O。

## 图片题目逐项补全

### 链表的通用陷阱与内核式设计

- 插入/删除前先处理空表、头节点、尾节点和仅一个节点四种边界；修改链接前保存必要指针，失败路径不改变原结构。
- 带头节点的链表让“在首元素前插入/删除”与普通节点操作统一；头节点可存长度等元数据，或仅作哨兵。
- 泛型容器常用 `void *` 保存对象地址、比较/释放函数指针描述行为；代价是类型检查弱、对象所有权必须明确。
- Linux 内核常将 `list_head` 嵌入业务结构体，用双向循环链表和“由成员地址反推宿主结构”的思想实现通用链表；这避免每种对象都重写链表节点管理。

### 适配器模式

- 适配器把不兼容接口转换为调用方需要的接口。数据结构题中常见：用两个栈实现队列、用两个队列实现栈。
- 关键不是背代码：说明“哪个结构负责输入、哪个负责输出”，以及在何时把元素搬运，确保 FIFO/LIFO 语义不变。

### 树、堆与排序

- 层序遍历使用队列：出队一个节点，依次把其非空孩子入队；空间最坏为一层宽度。
- BST 查找/插入/删除的复杂度与树高有关；删除有两个孩子的节点常用中序后继/前驱替换后再删。
- 堆是完全二叉树，常用数组保存；大根堆根最大。堆排序先建堆，再把堆顶与末尾交换并向下调整，时间 `O(n log n)`、额外空间 `O(1)`、通常不稳定。
